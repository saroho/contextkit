[project]
name = "contextkit"
version = "0.2.0"
description = "Simple memory system for AI coding workflow (.ai/ files)."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "ContextKit contributors" }]
keywords = ["ai", "cli", "memory", "qwen"]

[project.scripts]
contextkit = "contextkit:cli"

[tool.black]
line-length = 100
target-version = ["py310"]
